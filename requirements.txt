# Full stack for the pipeline, scripts, and Flask app.
# Versions are unpinned minimums (not the exact versions used during
# original development, which weren't recorded) — pin exact versions with
# `pip freeze > requirements.lock.txt` once the environment is verified.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
scikit-image>=0.21
joblib>=1.3
pandas>=2.0
Flask>=3.0

# Only needed for reproducing the U-Net notebooks locally instead of on
# Kaggle. Not required to run the RF pipeline, the web app's classical
# engine, or the tests. For the web app's optional local U-Net inference
# engine, see requirements-dl.txt instead.
# torch>=2.0
# opencv-python>=4.8
